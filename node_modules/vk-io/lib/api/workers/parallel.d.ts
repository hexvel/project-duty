import { SequentialWorker } from './sequential';
export declare class ParallelWorker extends SequentialWorker {
    protected execute(): Promise<void>;
    protected skipMethod(method: string): boolean;
}
